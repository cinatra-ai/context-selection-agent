# Dependencies (pnpm; this repo ships no committed lockfile — see ci.yml)
node_modules/
pnpm-lock.yaml

# Build output (tsconfig outDir)
dist/

# npm pack output
*.tgz

# Test coverage
coverage/

# Secrets — never commit
.env
.env.*
!.env.example

# Logs
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
